import { shallow } from "enzyme";
import DatosAnalisis from "../../../components/detalle/DatosAnalisis";

describe("Pruebas sobre DatosAnalisis.tsx", () => {
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {});
  test("debe rendirizar el componente correctamente", () => {
    const wrapper = shallow(<DatosAnalisis />);
    expect(wrapper).toMatchSnapshot();
  });
});
